services:
  - type: web
    name: streamverse
    runtime: node
    buildCommand: ""
    startCommand: "node server.js"
    healthCheckPath: /api/health
    envVars:
      - key: TMDB_KEY
        sync: false
      - key: WATCH_REGION
        value: IN
